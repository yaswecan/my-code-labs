services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules   # <-- garde les node_modules de l'image
    environment:
      - CHOKIDAR_USEPOLLING=true
      - CHOKIDAR_INTERVAL=100
      - WATCHPACK_POLLING=true
    depends_on:
      - backend
    networks:
      - app-network
    command: npm run dev -- --host

  backend:
    build: ./backend
    ports:
      - "4000:4000"
    environment:
      - DATABASE_URL=mysql://user:password@db:3306/monapp
      - PHP_MY_ADMIN_URL=http://phpmyadmin:80
    volumes:
      - ./backend:/app
      - /app/node_modules
      - ./sandbox:/app/sandbox
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      db:
        condition: service_started
      php-sandbox:
        condition: service_healthy
    networks:
      - app-network
    user: "1000:0"

  php-sandbox:
    build: ./php-sandbox
    image: php:8.2-cli
    container_name: php_sandbox
    stdin_open: true
    tty: true
    volumes:
      - ./sandbox:/sandbox
    command: tail -f /dev/null
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "php", "-v"]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 5s

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: monapp
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    volumes:
      - mysqldata:/var/lib/mysql
    ports:
      - "3306:3306"
    networks:
      - app-network

  phpmyadmin:
    image: phpmyadmin:latest
    restart: always
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_ARBITRARY: 1
      UPLOAD_LIMIT: 50M
    ports:
      - "8080:80"
    depends_on:
      - db
    networks:
      - app-network

volumes:
  mysqldata:

networks:
  app-network:
    driver: bridge

